{
  "name": "overlayer",
  "productName": "Overlayer",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "electron .",
    "package-win": "electron-packager . overlayer --overwrite --asar=true --platform=win32 --arch=ia32 --icon=icon.ico --prune=true --out=release-builds --version-string.CompanyName=CE --version-string.FileDescription=CE --version-string.ProductName=\"Overlayer\""
  },
  "author": "koin",
  "license": "MIT",
  "dependencies": {
    "ini": "^1.3.5"
  },
  "devDependencies": {
    "electron": "^7.2.4"
  }
}
